{
  "name": "pubpubtest01",
  "version": "1.0.0",
  "description": "test client api for pubpub",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "start": "npx tsx dist/index.js",
    "test": "mocha",
    "dev": "npx tsx watch src/index.ts",
    "clean": "npx tsc -b --clean && npx tsc -b --force",
    "run:debug": "NODE_OPTIONS=' --trace-warnings'  npx tsx src/index.ts >>importPub99.log 2>&1"
  },
  "author": "Nesim",
  "license": "ISC",
  "dependencies": {
    "esm": "^3.2.25",
    "pubpub-client": "^0.7.0",
    "textract": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "@types/textract": "^2.4.2",
    "concurrently": "^8.1.0",
    "nodemon": "^2.0.22",
    "tsx": "^3.12.7",
    "typescript": "^5.1.3"
  }
}
